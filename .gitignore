# Binaries (root-level only)
/spanner-etcd
/spanner-etcd-migrate
/spanner-etcd-linux-amd64
/spanner-etcd-migrate-linux-amd64
/migrate
/cmd/server/spanner-etcd
/cmd/migrate/spanner-etcd-migrate

# TLS certificates (never commit private keys)
certs/
*.key
*.crt
*.csr
*.pem
*.p12
*.srl

# Go vendor — generated, not committed.
# Run `make vendor` before `make docker` to populate.
/vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test artifacts
/tmp/
coverage.out
coverage.html

# macOS
.DS_Store
TESTING.local.md
