# Binary (root-level only, not helm chart subdirectory)
/spanner-etcd
/cmd/server/spanner-etcd

# TLS certificates (never commit private keys)
certs/
*.key
*.crt
*.csr
*.pem
*.p12
*.srl

# Go vendor directory — not committed; Docker builds use -mod=vendor
# with a pre-created vendor dir from CI, or use GOPROXY
/vendor/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Test artifacts
/tmp/
coverage.out
coverage.html

# macOS
.DS_Store
